{
  "name": "litepress",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.2.1",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "test": "turbo test",
    "generate:api-types": "pnpm --filter @litenova/api-types generate:api-types"
  },
  "devDependencies": {
    "turbo": "^2.0.14",
    "typescript": "^6.0.3"
  }
}
