services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: litepress
      POSTGRES_PASSWORD: litepress
      POSTGRES_DB: litepress
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U litepress"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
